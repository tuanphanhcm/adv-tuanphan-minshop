export const defaultLocale = "en" as const;
export const locales = ["en", "vn"] as const;
export const rtlLocales = ["en", "vn"] as const;
export const languageNames = {
  en: "English",
  vn: "Vietnam",
};
